runtime: nodejs20
instance_class: F2

automatic_scaling:
  min_instances: 0
  max_instances: 2
  target_cpu_utilization: 0.65

entrypoint: npm run start

env_variables:
  NODE_ENV: "production"
  PORT: "8080"
  ADMIN_USERNAME: "admin"
  ADMIN_PASSWORD: "adminjade2026"
  ADMIN_SESSION_SECRET: "jade_coatings_secret_session_key_2026_super_secure_token"
